git config --global user.name "Âàøå èìÿ"
git config --global user.email info@dka-develop.ru

We are at the heart of the project and write: git init
add all files: git add .
git commit -m "Init blog"
git remote show
git remote add origin and name SSH(Do not create a commit in the githab itself)
git push origin master
Login and password

Before edit
git add .
git push origin3 master
git commit -m "Creat admin dashboard"
git push origin3 master

